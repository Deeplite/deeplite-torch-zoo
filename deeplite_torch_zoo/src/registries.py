from deeplite_torch_zoo.utils.registry import Registry

VARIABLE_CHANNEL_BLOCKS = Registry(error_on_same_key=False)
EXPANDABLE_BLOCKS = Registry(error_on_same_key=False)
ZERO_COST_SCORES = Registry()
